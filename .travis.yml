sudo: false
language: node_js
node_js:
    - "stable"

after_script: "istanbul cover ./node_modules/mocha/bin/_mocha test/function/**.js --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage
"
